{
  "name": "jeff-barr-spotter",
  "version": "1.0.0",
  "description": "Superimpose Jeff Barr into selfies",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf .aws-sam",
    "deploy": "npm run clean && npm run build && sam build && sam deploy"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.126",
    "@types/node": "^20.8.0",
    "typescript": "^5.2.2"
  }
}
